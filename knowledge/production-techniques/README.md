# production-techniques/

Controllable production rules — the only knowledge area that directly drives editing decisions.
Every technique states its intended viewer behaviour and metric targets; techniques never claim
direct algorithmic effect (`CONTEXT.md`, "The two bodies of knowledge").

Planned canonical documents (created in phase 2 as evidence arrives — not pre-created empty):
`first-frame.md`, `pacing.md`, `overlays.md`, `captions.md`, `visual-motion.md`, `loops.md`,
`calls-to-action.md`, `audio.md`, `packaging.md`.

Currently seeded: `hooks.md` (vertical slice).

Document contract and editing rules: `../README.md`. Who writes: agents via reviewed PRs only.
Related structured records: `technique_record` artifacts in `research/sources/*/techniques/`.
