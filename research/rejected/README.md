# research/rejected/

Rejected sources and claims, **with reasons recorded** — nothing in this repository disappears
silently. Move the artifact here (keeping its ID) and record the rejection reason in its `notes`
plus `status: rejected` where the schema carries status. Who writes: the intake coordinator
(rejected sources) and the PR curator (claims rejected at review).

Empty right now: nothing has been rejected yet.
