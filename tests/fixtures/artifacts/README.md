# tests/fixtures/artifacts/

**Synthetic** artifacts proving schema coverage for record types the real data does not yet
exercise (contradictions, experiments). Scanned by the validators like any artifact directory —
so they must always validate — but they are test data, never evidence. IDs use the
`-test-fixture-` marker so they cannot be mistaken for real records.
