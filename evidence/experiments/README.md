# evidence/experiments/

`experiment_proposal` records (and later their results) created by `/propose-experiment`, via
PR. Schema: `schemas/experiment-proposal.schema.json`. Format proof: synthetic example in
`tests/fixtures/artifacts/exp-test-fixture-hook-length.yaml`.

Empty right now: experiments require a live account and analytics (phase 5); the vertical
slice's candidate experiments are recorded in the hypotheses' `proposed_actions`.
