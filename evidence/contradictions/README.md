# evidence/contradictions/

Standing `contradiction_record`s — unresolved or deliberately coexisting disagreements promoted
from `research/conflicts/` via PR. Rules: `../README.md`. Format proof: synthetic example in
`tests/fixtures/artifacts/contra-test-fixture-hook-length.yaml`.

Empty right now: no real contradictions exist yet (single-source store).
